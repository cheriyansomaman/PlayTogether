- I want to change the registration and login screen. Start registration by adding first and last names. On submit, generate automated username and save to the user document. Now username, firstname, lastname     
  should show and ask to enter passward also ask to confirm passward. once password confirmed. user can login with username and password. Login page must ask username first. On submitting username, check password is available or not. If available ask for password. If password is not available, ask to set password. If username itself not available, ask to register.
- Change the bulk member addition by admin by adding first and last names as mandatory. Optionally, age, address, team, can be added. On submit, generate automated username and save to the user document.

- Add sort on games. By default, sort on game name and age restriction(if available). Add option to sort on age restriction and game name as well. Add option to sort on team name as well.
- Add option to filter games. By default, show all games.
- if the game mode is of individual, keep Individual as Entry type for Record result. Similarly, for Team game mode, enable only Team entry type.
- for individual entry type, should see all participants in the dropdown.

- Record result table should show headers for rank/possition entry table and remove below card of "Col: Pos | Participant | Score | Time | Notes".
- Individual score entry should be added to the team score as well. So, if a participant won with 3 point in an individual game, add this 3 point to that particular individual and his/her team as well. So, in the team score, we can see the total score of the team which is the sum of all individual scores of that team. This will help us to determine the winning team based on the total score of all its members.